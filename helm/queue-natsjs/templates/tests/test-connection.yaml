apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "queue-natsjs.fullname" . }}-test-connection"
  labels:
    {{- include "queue-natsjs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "queue-natsjs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
